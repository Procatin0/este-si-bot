let config = {
  token: "TOKEN_BOT", // Reemplaze esto con el token de tu de bot.
  prefix: "P", // El prefix de su bot para indentificar sus comandos.

}

// Aqui exportamos el objeto config:
module.exports = config;
